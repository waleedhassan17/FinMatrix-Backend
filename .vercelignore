# Keep this minimal. Bare folder names here match ANY folder of that name in
# the tree (gitignore semantics), which previously excluded src/common/storage
# and crash-looped the function. Only list things that never appear under src/.
node_modules
.env
.env.*
*.log
